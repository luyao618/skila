---
name: bad-skill
---

# bad-skill

This skill is intentionally malformed: it is missing the required `description` field in the frontmatter.

The validator must reject this file with a VALIDATE_FAIL on the `description` field.
